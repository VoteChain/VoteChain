poll:
  - id: PRIMARY_KEY*
  - name: STRING*
  - desc: STRING*
  - role: STRING
  - creator: STRING*
  - startOn: STRING (ISO String)*
  - endOn: STRING (ISO String)*
  - createdOn: STRING (ISO String)*
  - limit: INTEGER
  - passcode: STRING (HASH)
  #   - status: ENUM (active | upcomming | ended) ?
  - candidates:
      - id:
      - name:
  - parties:
      - id:
      - name:
  #   - voters:
  #       - id:

  - type: STRING*(by candidate / by candidate or party)
  - eligibility: ENUM (all | token_holders? | verified | custom)*
  - eligibleVoters:
      - username:
#   - visibility: ENUM (public | private) ?

vote:
  - id:
  - pollId:
  - voter:
  - party:
